# ProductRadar - AI Agent

ИИ-агент для автоматического поиска и анализа вирусных косметических товаров на платформах Douyin, TikTok, Xiaohongshu и других.

## 🎯 Основные возможности

- 🔍 **Автоматический поиск** вирусных бьюти-продуктов на 4+ платформах
- 🌐 **Приоритет Douyin** - основной источник (20-30 продуктов из китайского рынка)
- 🤖 **AI-перевод** - автоматический перевод с китайского на русский
- 📊 **Метрики и аналитика** - расчет ER, UGC, trendScore (0-100)
- 📁 **Экспорт в CSV** - структурированные данные с автозагрузкой на Google Drive
- ✅ **Дедупликация** - умное удаление дубликатов

## Установка

### С использованием Poetry (рекомендуется)
```bash
poetry install
poetry shell
```

### С использованием pip
```bash
python -m venv venv
# Windows
.\venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

pip install -r requirements.txt
```

## Структура проекта

```
ProductRadar/
├── src/              # Исходный код агента
│   ├── agents/       # Модули агентов
│   ├── models/       # ML модели
│   ├── utils/        # Утилиты
│   └── config/       # Конфигурации
├── tests/            # Тесты
├── data/             # Данные
│   ├── raw/          # Сырые данные
│   └── processed/    # Обработанные данные
├── notebooks/        # Jupyter notebooks для экспериментов
├── scripts/          # Скрипты для запуска
└── docs/             # Документация
```

## Использование

### Быстрый запуск

```bash
# Базовый запуск (30-50 продуктов)
python scripts/run_agent.py

# С кастомными параметрами
python scripts/run_agent.py --min-products 40 --target-products 60 --douyin-target 30

# С Google Drive интеграцией
python scripts/run_agent.py --google-creds google-credentials.json
```

### Программное использование

```python
from src.agents import ProductRadarAgent

# Создаем агента
config = {
    'min_products': 30,
    'target_products': 50,
    'douyin_target': 25,
    'douyin_priority': True
}

agent = ProductRadarAgent(config=config)

# Запускаем
result = await agent.run()

print(f"Найдено продуктов: {result['total_products']}")
print(f"Файл CSV: {result['export']['local_path']}")
print(f"Google Drive: {result['export']['drive_url']}")
```

### Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--min-products` | Минимум продуктов | 30 |
| `--target-products` | Целевое количество | 50 |
| `--douyin-target` | Цель с Douyin | 25 |
| `--no-douyin-priority` | Без приоритета Douyin | False |
| `--google-creds` | Path к credentials | None |

## Разработка

Для разработки установите дополнительные зависимости:
```bash
poetry install --with dev
```

## Тестирование

```bash
pytest tests/
```

