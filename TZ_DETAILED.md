# 🎯 ПОЛНЫЙ КОНТЕКСТ ЗАДАЧИ: Сбор данных Pipiads в Google Sheet

## ОСНОВНАЯ ЗАДАЧА

Создать автоматизацию, которая собирает данные о товарах и TikTok видео с сайта Pipiads и заполняет Google Sheet.

### Входные данные:

- **Email аккаунта Pipiads:** aiagentforcosmobeauty@gmail.com
- **Пароль Pipiads:** )ZRg8,tbOtsR>)U (обратите внимание: начинается со скобки, пароль без пробелов)
- **ID Google Sheet:** 1VJMixODvnIPBf7EjFoJ8XMH1lepycVlXREKQI7MVxWQ
- **Название листа в таблице:** "шаблон выгрузуи 1.0" (ИМЕННО ЭТО НАЗВАНИЕ, не GID)
- **Начальный URL Pipiads:** https://www.pipiads.com/ru/tiktok-shop-product?time=7&categorize=601595,601733,873480,601613,601615,601616,601619,601602,601608,601609,601611,601610,601646,873736,601506,601511,2169232,978056,601490,601493,601492,601495,601494,601498,1003784,873864,873608,601686,601565,1004808,601627,601681,601644,601513,601516,981512,601469,806160,700789,1004168,601582,601585,601587,601586,601588,853264,601558,601560,601550,601552,601554,601556,601555,601529,601534,853520,601618,875144,601462,601461,601463,601737,874888&search_type=1&sales_trend=1&current_page=1&page_size=20&sort=5&sort_type=desc

### Что нужно собрать:

- Первые 3 товара со страницы поиска Pipiads
- Для каждого товара: название, категория, ссылка на страницу товара
- Для каждого товара: ровно 3 TikTok видео из раздела "TikTok Ads"
- Для каждого видео: ссылка, количество показов (impressions), сценарий, hook, возраст аудитории, страна, дата первого просмотра

---

## 📊 СТРУКТУРА GOOGLE SHEET (очень важно!)

### Столбцы таблицы:

- **A:** Номер товара (1, 2, 3) - заполняет автоматизация
- **B:** Product Name - название товара (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **C:** Перевод названия продукта - НЕ трогать! Это для другого ИИ
- **D:** Category - категория товара (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **E:** Pipiads Link - ссылка на страницу товара, формат: https://www.pipiads.com/tiktok-shop-product/<ID> (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **F:** TikTok Post first video - ссылка на первое видео (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **G:** Impression first video - количество показов первого видео (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **H:** Script first video - сценарий первого видео (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **I:** Hooks first video - hook первого видео (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **J:** Audience first video - возраст аудитории + платформа, формат: "35-45 Android" (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **K:** Country/Region first video - страна (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **L:** First seen first video - дата в формате "Oct 27 2025" (ЗАПОЛНЯЕТ АВТОМАТИЗАЦИЯ)
- **M-S:** То же самое для второго видео
- **T-Z:** То же самое для третьего видео

### КРИТИЧЕСКИ ВАЖНО:

- Строка 2 содержит пример данных - **НЕ ПЕРЕПИСЫВАТЬ!**
- Данные автоматизации пишутся начиная со строки 3 (для 1-го товара), строка 4 (для 2-го товара), строка 5 (для 3-го товара)
- **НИКОГДА не трогать столбцы A и C** - они либо для номеров, либо для другого ИИ

---

## 🔄 АЛГОРИТМ ДЕЙСТВИЙ (пошагово)

### ШАГ 1: Инициализация и вход в аккаунт

1. Загрузить начальный URL Pipiads с фильтрами
2. Проверить, вошёл ли пользователь в аккаунт через pageQuery с вопросом "Is the user currently signed in?"
3. Если не вошёл - ввести email и пароль, нажать кнопку входа
4. Проверить, что вход успешен (ещё один pageQuery)

### ШАГ 2: Получить список товаров

1. Использовать pageQuery для извлечения первых 3 товаров со страницы поиска
2. Для каждого товара получить:
   - `name` - название товара
   - `category` - категория
   - `url` - полная ссылка на страницу товара

### ШАГ 3: Для каждого из 3 товаров

1. Загрузить страницу товара через `window.loadUrl(product.url)`
2. На странице товара найти раздел "TikTok Ads"
3. Нажать на dropdown "Sort by" и выбрать "First seen" (сортировка по дате первого просмотра)
4. Использовать pageQuery для извлечения видео с фильтрами:
   - Обязательный фильтр: First seen дата должна быть не старше 7 дней от сегодня
   - Приоритет: сначала видео с impression >= 100,000, потом с impression >= 50,000
   - Нужно получить 3 видео для каждого товара
5. Для каждого видео получить:
   - `videoLink` - ссылка на видео
   - `adSearchUrl` - ссылка на страницу деталей видео (ad-search)
   - `impressions` - количество показов
   - `firstSeen` - дата первого просмотра в формате "Oct 27 2025"

### ШАГ 4: Для каждого видео - получить детальные метрики

1. Загрузить страницу ad-search через `window.loadUrl(video.adSearchUrl)`
2. Использовать pageQuery для извлечения:
   - `tiktokPostLink` - ссылка на пост TikTok (из поля "Пост TikTok")
   - `impressions` - количество показов (**ИЩЕМ ИМЕННО "Показы", а не "Нравится"!**)
   - `script` - текст сценария из секции "Анализ транскрипта" (если нет - "N/A")
   - `hook` - hook из секции Hook (если нет - "N/A")
   - `audienceAge` - возраст аудитории из Target Audience (если нет - "N/A")
   - `country` - страна из Target Audience (если нет - "N/A")
   - `firstSeen` - дата в формате "Oct 27 2025"

### ШАГ 5: Фильтр по датам (КРИТИЧНО!)

После получения видео со страницы товара **ОБЯЗАТЕЛЬНО** проверить, что дата первого просмотра не старше 7 дней от сегодня. Удалить видео, которые старше.

**СЕГОДНЯШНЯЯ ДАТА** на момент последнего теста: 31 октября 2025  
Значит, берём видео от 24 октября 2025 и позже. Видео старше (например, из марта, сентября, января) - **НЕ БРАТЬ!**

### ШАГ 6: Заполнение Google Sheet

1. Для каждого товара заполнить одну строку в таблице
2. Используя session.service с сервисом "google-sheet":
   - Sheet ID: из параметра googleSheetId
   - Sheet Name: **ИМЕННО "шаблон выгрузуи 1.0"** (в кавычках!)
   - Row: rowIndex (строка 3 для товара 1, строка 4 для товара 2, строка 5 для товара 3)
3. Заполнить ячейки согласно структуре выше

---

## ❌ ОШИБКИ И ИХ ИСПРАВЛЕНИЕ

### ОШИБКА 1: Перезапись пример-строки

**Проблема:** Первый раз я переписал строку 2 (пример), вместо того чтобы писать в строку 3.

**Решение:**
- Проверить, что `rowIndex = productIndex + 3` (не productIndex + 2)
- Строка 3 для товара с индексом 0, строка 4 для индекса 1, строка 5 для индекса 2
- **НИКОГДА не трогать строку 2**

### ОШИБКА 2: Неправильный лист в Google Sheet

**Проблема:** Использовал GID (143310243) вместо имени листа. Ошибка в логах: "Error: Missing required information: The actual sheet name corresponding to sheet ID 143310243 is missing."

**Решение:**
- В функции `fillGoogleSheetRow` использовать: `const sheetName = "шаблон выгрузуи 1.0";`
- В session.service prompt писать: `on sheet "${sheetName}"` (с кавычками в строке!)
- Именно это название листа, не GID

### ОШИБКА 3: Столбцы A и C

**Проблема:** Я пытался заполнять столбец A (номер товара) правильно, но нужно было убедиться.

**Решение:**
- Столбец A: Заполнить номером товара (1, 2, 3) через: `"A": String(productIndex + 1)`
- Столбец C: **ВООБЩЕ НЕ ТРОГАТЬ** - это для другого ИИ
- Функция `fillGoogleSheetRow` должна принимать параметр `productIndex` и использовать его для столбца A

### ОШИБКА 4: Неправильная обработка столбца J, Q, X (Audience)

**Проблема:** Столбец "Audience" должен содержать возраст И платформу, например: "35-45 Android"

**Решение:**
- Получать две части: `audienceAge` и `platform` (если платформа есть)
- Объединять: `${productData.videos[0]?.audienceAge || "N/A"} ${productData.videos[0]?.platform || ""}` и обрезать `.trim()`
- Пока платформа не извлекается из Pipiads, просто оставлять возраст

### ОШИБКА 5: Старые видео берутся вместо свежих

**Проблема:** Фильтр pageQuery на странице товара не может правильно отфильтровать видео по дате. В результате берутся видео из марта, сентября, января вместо видео последних 7 дней.

**Решение:**

После получения видео со страницы товара через pageQuery, **ОБЯЗАТЕЛЬНО фильтровать в коде:**

```typescript
const today = new Date();
const sevenDaysAgo = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1000);

const filteredVideos = videos.filter(video => {
  const videoDate = parseVideoDate(video.firstSeen);
  const isWithinSevenDays = videoDate && videoDate >= sevenDaysAgo;
  return isWithinSevenDays;
});
```

Использовать функцию `parseVideoDate` для парсинга дат в формате "Oct 27 2025":

```typescript
function parseVideoDate(dateString: string): Date | null {
  if (!dateString || dateString === "N/A") {
    return null;
  }
  const months: { [key: string]: number } = {
    "Jan": 0, "Feb": 1, "Mar": 2, "Apr": 3, "May": 4, "Jun": 5,
    "Jul": 6, "Aug": 7, "Sep": 8, "Oct": 9, "Nov": 10, "Dec": 11
  };
  const parts = dateString.trim().split(" ");
  if (parts.length !== 3) {
    return null;
  }
  const monthStr = parts[0];
  const dayStr = parts[1];
  const yearStr = parts[2];
  const month = months[monthStr];
  const day = parseInt(dayStr, 10);
  const year = parseInt(yearStr, 10);
  if (month === undefined || isNaN(day) || isNaN(year)) {
    return null;
  }
  return new Date(year, month, day);
}
```

После фильтрации использовать `filteredVideos` вместо `videos` в основном цикле

### ОШИБКА 6: Неправильное извлечение impressions из ad-search страницы

**Проблема:** pageQuery путал "Impressions" (Показы) с "Likes" (Нравится) на странице ad-search, возвращая маленькие числа (168, 47, 28) вместо реальных (101300, 13200, 15100).

**Решение:**
- В pageQuery для ad-search страницы использовать очень специфичный prompt:
  - "Extract from the ad details page: ..., Impression number (the value labeled as 'Показы' with the large number like 15.1K), ..."
- **ОБЯЗАТЕЛЬНО упомянуть**, что это "Показы", а не "Нравится"
- Указать, что это большое число вроде "15.1K", а не маленькое

### ОШИБКА 7: TypeScript ошибка типа AirtopSession

**Проблема:** error TS2304: Cannot find name 'AirtopSession' при использовании `session: AirtopSession` в сигнатуре функции.

**Решение:** Использовать `session: any` вместо конкретного типа

### ОШИБКА 8: Неправильная навигация к товарам через click

**Проблема:** Попытки нажимать на товары через `window.click()` были ненадёжны и не всегда приводили на страницу товара.

**Решение:**
- Вместо нажатия использовать прямую загрузку URL:
  - Сначала извлечь URL товара через pageQuery
  - Потом использовать `window.loadUrl(productUrl)` для навигации
- Это намного надёжнее и быстрее

---

## 🔑 КЛЮЧЕВЫЕ МОМЕНТЫ ДЛЯ ПАМЯТИ

1. **Дата фильтрации очень важна!** Сегодня: 31 октября 2025 → берём видео от 24 октября и позже. Если видео из марта или сентября - это ОШИБКА. Нужна фильтрация в коде, не в pageQuery.

2. **Google Sheet лист называется "шаблон выгрузуи 1.0"** - это точное название, нужно использовать его в session.service промпте.

3. **Столбцы A и C не трогать:**
   - A - для номеров товаров
   - C - для перевода от другого ИИ
   - Остальные столбцы (B, D, E, F-Z) - для автоматизации

4. **Impressions на ad-search странице** - очень важно правильно извлекать "Показы" (большое число), а не "Нравится" (маленькое число). Prompt должен быть очень специфичным.

5. **Дата в формате "Oct 27 2025"** - это оригинальный формат с Pipiads, не преобразовывать в YYYY-MM-DD.

6. **Функция fillGoogleSheetRow** должна принимать параметр `productIndex`, чтобы правильно заполнить столбец A и строку таблицы.

7. **Если видео нет некоторых данных** (script, hook, age, country) - писать "N/A", не оставлять пусто.

8. **Прямая загрузка URL через window.loadUrl()** - это надёжнее, чем нажатие на элементы.

9. **Фильтрация видео по дате должна быть в КОДЕ** (TypeScript), после получения видео от pageQuery, а не в самом pageQuery промпте.

10. **При ошибках в Google Sheet** - проверить, что используется именно имя листа, а не GID.

---

## 📋 ТЕКУЩЕЕ СОСТОЯНИЕ КОДА

✅ Логика входа в аккаунт работает  
✅ Извлечение 3 товаров со страницы поиска работает  
✅ Навигация на страницу товара через window.loadUrl() работает  
✅ Сортировка видео по "First seen" работает  
✅ Извлечение видео со страницы товара работает  
✅ Фильтрация видео по датам в коде работает (добавлена в последней версии)  
✅ Извлечение деталей видео со страницы ad-search работает  
✅ Заполнение Google Sheet работает с правильным названием листа  
✅ Обработка 3 товаров (не 1) работает  

**Последний тест (31 октября 2025):** Обработано 3 товара, но только для 1-го товара найдено видео в пределах 7 дней. Это ожидаемо, так как большинство видео действительно старые.

---

## 🚀 ЕСЛИ КОНТЕКСТ ПОТЕРЯЕТСЯ И НУЖНО НАЧАТЬ ЗАНОВО

Проверить файлы в `/work/57a5b4f1-efd8-4bb4-9272-fc2224b04918/workflows/`:

- `workflow__57a5b4f1-efd8-4bb4-9272-fc2224b04918.ts` - основной код
- `parameter_schema__57a5b4f1-efd8-4bb4-9272-fc2224b04918.json` - параметры
- `parameters_preconfigured__57a5b4f1-efd8-4bb4-9272-fc2224b04918.json` - предзаполненные значения
- `CODE_PLAN__57a5b4f1-efd8-4bb4-9272-fc2224b04918.md` - план реализации
- `NOTES__57a5b4f1-efd8-4bb4-9272-fc2224b04918.md` - заметки

### ОБЯЗАТЕЛЬНО:

1. Использовать лист **"шаблон выгрузуи 1.0"** в Google Sheet
2. Не трогать столбцы A и C
3. Писать в строки 3, 4, 5 (начиная со строки 3)
4. Фильтровать видео по датам **В КОДЕ** (не в pageQuery)
5. Использовать прямую загрузку URL для навигации

### Если видео не берутся правильно:

1. Проверить фильтр по датам (сегодня - 7 дней)
2. Проверить, что в pageQuery для ad-search используется "Показы", а не "Нравится"
3. Проверить, что используется `filteredVideos`, а не просто `videos`

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ ЗАМЕТКИ

- Формат даты: "Oct 27 2025" (не преобразовывать)
- Приоритет видео: сначала >= 100,000 impressions, потом >= 50,000
- Минимальный порог: impression >= 50,000
- Период: последние 7 дней от текущей даты
- Если данных нет - писать "N/A"
- Столбец J, Q, X (Audience): формат "35-45 Android" (возраст + платформа)

