Pipiads TikTok Video Data Extraction Automation - Подробный Обзор
📋 Описание Проекта

Это Airtop-автоматизация для сбора данных о TikTok-видеорекламе с платформы Pipiads и заполнения Google Sheet выбранными данными.

Основная задача: Извлечь информацию о TikTok-видео (даты, количество показов, скрипты, хуки, аудиторию) для 5 косметических продуктов с фильтрацией по минимальному количеству показов (2000) и датам (последние 90 дней).

Статус: В разработке - требует исправления ошибок pageQuery при передаче outputSchema параметра
🏗️ Архитектура и Поток Данных
Общий Процесс:

    Инициализация → Загрузка параметров, проверка авторизации на Pipiads
    Поиск Продуктов → Парсинг списка продуктов со страницы поиска
    Обработка Каждого Продукта → Загрузка страницы продукта
    Извлечение Видео → Получение всех видеокарточек в разделе "TikTok Ads"
    Фильтрация → По дате (90+ дней) и количеству показов (2000+)
    Сбор Деталей → Извлечение скриптов, хуков, информации об аудитории
    Сохранение → Заполнение Google Sheet через API
    Отчет → Логирование результатов и ошибок

🔧 Ключевые Компоненты
1. Параметры Автоматизации

{
  "googleSheetId": "string (required)",
  "pipiadsEmail": "string (required)",
  "pipiadsPassword": "string (required)",
  "minImpressions": "number (default: 2000)",
  "numberOfProducts": "number (default: 5)"
}

2. Основные SDK Вызовы
PageQuery для Извлечения Видео

    Назначение: Получить информацию о видеокарточках (дата, количество показов, ссылка)
    Формат Дат: YYYY-MM-DD (например, 2025-09-11)
    Формат Показов: Числовое значение (2M → 2000000, 263.4K → 263400)
    Критическая Проблема: outputSchema передается как второй параметр, но SDK ожидает иную структуру

Service Calls для Google Sheets

    Сервис: google-sheet
    Действие: Обновление строк с данными о продуктах
    Формат: Заполнение колонок A-AN (до 5 видео на продукт)

3. Логирование

Подробное логирование на каждом этапе для отладки:

    Инициализация параметров
    Проверка авторизации
    Извлечение каждого продукта
    Фильтрация видео
    Ошибки парсинга и обработки
    Результаты сохранения в Sheet

🐛 Выявленные Проблемы и Опыт Решения
Проблема 1: Формат Передачи OutputSchema в PageQuery ⚠️ КРИТИЧНА

Симптомы:

    Ошибка: Cannot read properties of undefined (reading 'outputSchema')
    Компилируется успешно, но падает при выполнении

Причина:
Неправильный формат передачи outputSchema в pageQuery. В коде передается как:

await window.pageQuery(
  "prompt",
  {
    "$schema": "...",
    type: "object",
    properties: {...}
  }
)

Но SDK может ожидать другую структуру (возможно, с обёрткой configuration).

Попытанные Решения:

    ✗ Передача schema как второго параметра (вызывает ошибку outputSchema)
    ✗ Оборачивание в configuration объект
    ✗ Использование as any для обхода типизации
    ✓ Успешно работает без schema параметра (но требует явного JSON в prompt)

Рекомендация для Следующего Этапа:
Проверить документацию AirtopWindow.pageQuery в /work/57a5b4f1-efd8-4bb4-9272-fc2224b04918/airtop-docs/consolidated/AirtopWindow__pageQuery.md для правильного синтаксиса передачи outputSchema.
Проблема 2: Непоследовательная Работа PageQuery при Извлечении Видео

Симптомы:

    Один и тот же pageQuery для первого продукта возвращает видео во время ручного тестирования
    При запуске в автоматизации возвращает 0 видео для всех продуктов

Причина:
Вероятно, связано с ошибкой в передаче outputSchema - когда вызов падает, возвращается пустой результат, который парсится как пустой массив видео.

Опыт Ручного Тестирования ✓ УСПЕШНО:

// Эта команда работает в EXPLORATION phase:
const response = await window.pageQuery(
  "Find all TikTok video cards...",
  {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {"videos": {...}}
  }
);

// Возвращает:
{
  "videos": [
    {"date": "2025-09-11", "impressions": 2000000, "videoLink": "..."},
    {"date": "2025-08-20", "impressions": 263400, "videoLink": "..."},
    {"date": "2025-07-04", "impressions": 38800000, "videoLink": "..."}
  ]
}

Проблема 3: Фильтрация Видео по Датам ✓ РЕШЕНА

Исходная Реализация:

    Парсинг дат в формате "Sep 11 2025" с английскими названиями месяцев
    Использование Date объектов для сравнения

Найденные Проблемы:

    Видео возвращают даты в разных форматах (иногда с диапазонами: "Sep 11 2025-Nov 05 2025")
    parseDate функция брала только первую дату из диапазона
    Некорректное сравнение Date объектов в некоторых случаях

Решение:

    Изменён формат дат на YYYY-MM-DD для более надёжного парсинга
    Простое строковое сравнение вместо работы с Date объектами
    Добавлена функция formatDateForComparison() для конвертации текущей даты

Проблема 4: Неправильная Типизация Параметров ✓ РЕШЕНА

Симптомы:

    minImpressions был undefined в первых тестах
    Логи показывали: "impression >= undefined"

Причина:
Параметры приходят как строки, но код ожидает числа. Деструктуризация не конвертирует типы автоматически.

Решение:

const minImpressions = typeof params.minImpressions === 'number'
  ? params.minImpressions
  : 2000;

📊 Результаты Тестирования
Успешные Этапы:

    ✅ Загрузка Pipiads URL
    ✅ Проверка авторизации (user signed in correctly)
    ✅ Извлечение списка продуктов (20 продуктов найдено)
    ✅ Загрузка страниц отдельных продуктов
    ✅ Ручное тестирование pageQuery (извлекает видео корректно)

Неудачные Этапы:

    ❌ PageQuery с outputSchema в коде (все попытки падают с "Cannot read properties of undefined")
    ❌ Извлечение видео в автоматизации (возвращает пустой массив из-за ошибки в pageQuery)
    ❌ Фильтрация и сохранение (не выполняется, так как видео не извлекаются)

Лог Ошибок:

pageQuery Failure: Cannot read properties of undefined (reading 'outputSchema')

Эта ошибка повторяется для каждого продукта при попытке извлечь видео.
🔍 Полученный Опыт и Insights
1. PageQuery - Критические Параметры

    Обязателен outputSchema в правильном формате (JSON Schema draft-07)
    Корневой тип должен быть "object"
    Неправильная передача параметров → молчаливый отказ или ошибка типа

2. Дата-Фильтрация на Pipiads

    Видео могут иметь диапазоны дат (дата начала - дата конца)
    Для фильтрации нужна только начальная дата
    Формат: обычно "Mmm DD YYYY" (Sep 11 2025), но может варьироваться

3. Структура Видеокарточек

На странице продукта видео находятся в разделе "TikTok Ads" и содержат:

    Дата (начало показа объявления)
    Количество показов (в форматах: 2M, 263.4K, 38.8M, 944.9K и т.д.)
    Видеоссылка (URL на MP4 файл на CDN)
    Дополнительные данные (скрипт, хук, возраст аудитории, страна) - доступны через дополнительный pageQuery

4. Google Sheets Integration

    Использует service интеграцию с "google-sheet"
    Требует sheet ID (не URL)
    Поддерживает естественное описание обновлений ("Fill row X with...")
    Работает через session.service() в коде

🛠️ Что Нужно Исправить Следующему AI
1. КРИТИЧЕСКИЙ ПРИОРИТЕТ: Исправить PageQuery Schema

    Изучить /work/57a5b4f1-efd8-4bb4-9272-fc2224b04918/airtop-docs/consolidated/AirtopWindow__pageQuery.md
    Найти правильный синтаксис передачи outputSchema
    Обновить обе вызовы pageQuery в workflow (линии ~92 и ~172)
    Возможные варианты:
        Передача как второй параметр без обёртки
        Передача через конфигурационный объект
        Другой синтаксис, зависящий от версии Airtop SDK

2. Тестирование в EXPLORATION Phase

    Загрузить продукт Pipiads
    Вручную протестировать правильный синтаксис pageQuery
    Убедиться, что видео извлекаются корректно
    Документировать рабочий синтаксис

3. После Исправления Schema

    Запустить полный цикл тестирования
    Проверить фильтрацию по датам и количеству показов
    Убедиться, что данные сохраняются в Google Sheet
    Проверить все 5 продуктов обрабатываются корректно

4. Потенциальные Улучшения

    Добавить обработку edge cases (видео без некоторых полей)
    Добавить retry логику для нестабильных pageQuery вызовов
    Оптимизировать количество API вызовов к Google Sheets
    Добавить проверку корректности данных перед сохранением

📁 Структура Файлов Проекта

/work/57a5b4f1-efd8-4bb4-9272-fc2224b04918/
├── workflows/
│   ├── workflow__57a5b4f1-efd8-4bb4-9272-fc2224b04918.ts    # Основной код (250+ строк)
│   ├── parameter_schema__57a5b4f1-efd8-4bb4-9272-fc2224b04918.json
│   ├── parameters_preconfigured__57a5b4f1-efd8-4bb4-9272-fc2224b04918.json
│   └── PROJECT_OVERVIEW.md                           # Этот файл (теперь в файловой системе)
├── airtop-docs/
│   └── consolidated/                                 # Документация SDK
│       ├── AirtopWindow__pageQuery.md                # КРИТИЧНА ДЛЯ ИСПРАВЛЕНИЯ
│       ├── AirtopSession__service.md
│       └── ... (другие методы)
└── logs/
    └── log__[execution-id].jsonl                     # Логи последних запусков

🧪 Как Воспроизвести Ошибку

    Убедитесь, что пользователь авторизован на Pipiads
    Запустите workflow с параметрами:

    googleSheetId: "1VJMixODvnIPBf7EjFoJ8XMH1lepycVlXREKQI7MVxWQ"
    pipiadsEmail: "aiagentforcosmobeauty@gmail.com"
    pipiadsPassword: "***"
    minImpressions: 2000
    numberOfProducts: 5

    Проверьте логи - увидите "pageQuery Failure: Cannot read properties of undefined (reading 'outputSchema')"

💡 Ключевые Учения

    PageQuery Требует Точной Синтаксиса: Даже небольшие отклонения от ожидаемого формата вызывают ошибки
    Ручное Тестирование Критично: EXPLORATION фаза помогла выявить, что pageQuery РАБОТАЕТ при правильном синтаксисе
    Логирование Спасает: Подробные логи на каждом этапе помогают быстро локализовать проблему
    Типизация Параметров: Требуется явная конверсия типов при работе с пользовательскими параметрами
    Date Handling: Строковое сравнение YYYY-MM-DD надёжнее, чем работа с Date объектами

📌 Резюме для Следующего AI

Что работает ✅:

    Вся инфраструктура Airtop (загрузка URLs, авторизация, логирование)
    Извлечение списка продуктов
    Ручное тестирование pageQuery (WORKS при правильном синтаксисе!)

Что не работает ❌:

    PageQuery вызовы в коде (ошибка outputSchema)
    Извлечение видео в автоматизации

Решение:

    Найти правильный синтаксис передачи outputSchema в pageQuery
    Это однозначно исправит всю цепочку обработки видео

Статус Кода:

    Компилируется успешно
    Логирование детальное
    Структура чистая и понятная
    Требуется только одно исправление!
