# Технический план реализации парсинга Pipiads

## 🎯 Выбранный вариант: Playwright + Google Sheets API + Python

### Почему эта комбинация:
- **Playwright** — надежная браузерная автоматизация для JS-сайтов
- **Google Sheets API** — прямая запись без ручного копирования
- **Python** — простота разработки и богатая экосистема библиотек
- **Дополнительные компоненты** — обработка ошибок, логирование, retry-механизмы

---

## 📐 Архитектура решения

### Компоненты системы:

```
┌─────────────────────────────────────────────────────────┐
│                    Main Script (main.py)                 │
│  - Координация всех процессов                            │
│  - Управление workflow                                   │
│  - Обработка ошибок и логирование                        │
└───────────────────┬─────────────────────────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
┌───────────┐ ┌───────────┐ ┌───────────┐
│  Browser  │ │  Parser   │ │  Sheets   │
│  Manager  │ │  Engine   │ │  Writer   │
└───────────┘ └───────────┘ └───────────┘
```

### 1. **Browser Manager** (browser_manager.py)
**Назначение:** Управление браузером и сессией

**Функционал:**
- Инициализация Playwright (headless или headful режим)
- Авторизация на Pipiads
- Управление cookies и сессией
- Обработка таймаутов и retry при ошибках
- Сохранение состояния сессии (избежание повторных авторизаций)

**Технологии:**
- `playwright` (Python)
- `asyncio` для асинхронности
- Cookie storage для сохранения сессии

---

### 2. **Parser Engine** (parser_engine.py)
**Назначение:** Извлечение данных со страниц Pipiads

**Модули:**

#### 2.1. **Product List Parser**
- Парсинг списка товаров с главной страницы
- Извлечение ссылок на товары
- Обработка пагинации (если нужно больше товаров)

#### 2.2. **Product Page Parser**
- Переход на страницу товара
- Извлечение: Product Name, Category
- Переход в блок "TikTok Ads"
- Установка сортировки "First seen"

#### 2.3. **Video Filter & Selector**
- Фильтрация видео по критериям:
  - **Приоритет:** сначала `impression >= 100,000`, потом `impression >= 50,000`
  - `First seen <= (сегодня - 7 дней)` - **КРИТИЧНО: фильтрация в КОДЕ, не в селекторах!**
- Выбор топ-3 видео, соответствующих критериям
- Обработка случая, когда видео недостаточно
- **Важно:** Фильтрация по дате должна происходить в коде после получения данных, используя функцию `parseVideoDate()` для парсинга формата "Oct 27 2025"

#### 2.4. **Video Details Parser**
- Переход в карточку видео → "More detail" (через прямую загрузку URL `window.loadUrl()`)
- Извлечение данных со страницы ad-search:
  - TikTok видео (ссылка из поля "Пост TikTok")
  - **Охват (impression)** - **КРИТИЧНО: извлекать "Показы", а НЕ "Нравится"!** (большое число типа 15.1K, не маленькое)
  - Сценарий (Transcript) - из секции "Анализ транскрипта" (если нет - "N/A")
  - Хук (Hook) - из секции Hook (если нет - "N/A")
  - Возраст ЦА - из Target Audience (если нет - "N/A")
  - Страна - из Target Audience (если нет - "N/A")
  - Дата ролика (First seen) - **формат "Oct 27 2025" (НЕ преобразовывать в YYYY-MM-DD!)**

**Технологии:**
- `playwright` для навигации
- **Прямая загрузка URL** через `page.goto()` вместо кликов (надежнее)
- `beautifulsoup4` для парсинга HTML (альтернатива, если нужен)
- `re` для обработки регулярных выражений
- `dateutil` для работы с датами
- **Функция parseVideoDate()** для парсинга формата "Oct 27 2025":
```python
def parse_video_date(date_string: str) -> Optional[datetime]:
    """Парсит дату в формате 'Oct 27 2025'"""
    if not date_string or date_string == "N/A":
        return None
    months = {
        "Jan": 1, "Feb": 2, "Mar": 3, "Apr": 4, "May": 5, "Jun": 6,
        "Jul": 7, "Aug": 8, "Sep": 9, "Oct": 10, "Nov": 11, "Dec": 12
    }
    parts = date_string.strip().split()
    if len(parts) != 3:
        return None
    month_str, day_str, year_str = parts
    month = months.get(month_str)
    day = int(day_str)
    year = int(year_str)
    if month is None or not (1 <= day <= 31) or year < 2020:
        return None
    return datetime(year, month, day)
```

---

### 3. **Sheets Writer** (sheets_writer.py)
**Назначение:** Запись данных в Google Sheets

**Функционал:**
- Аутентификация через Google Sheets API
- **КРИТИЧНО:** Использование листа **"шаблон выгрузуи 1.0"** (не GID!)
- Запись данных в строки 3, 4, 5 (для товаров 1, 2, 3 соответственно)
- **НИКОГДА не трогать строку 2** (пример данных)
- **НИКОГДА не трогать столбец C** (для другого ИИ)
- Запись данных в правильном порядке столбцов (A, B, D, E, F-Z)
- Валидация данных перед записью
- Обработка ошибок API

**Структура столбцов:**
- A: Номер товара (1, 2, 3)
- B: Product Name
- C: **НЕ ТРОГАТЬ** (для другого ИИ)
- D: Category
- E: Pipiads Link
- F-L: Данные первого видео
- M-S: Данные второго видео
- T-Z: Данные третьего видео

**Технологии:**
- `google-api-python-client` (Google Sheets API)
- `google-auth` для OAuth2
- `google-auth-oauthlib` для токенов

**Структура данных:**
```python
{
    "product_index": int,  # 1, 2, 3
    "product_name": str,
    "category": str,
    "pipiads_link": str,  # формат: https://www.pipiads.com/tiktok-shop-product/<ID>
    "video_1": {
        "tiktok_link": str,
        "impression": int,  # >= 50,000 (приоритет >= 100,000)
        "scenario": str,  # "N/A" если нет
        "hook": str,  # "N/A" если нет
        "audience": str,  # формат: "35-45 Android" (возраст + платформа)
        "country": str,  # "N/A" если нет
        "first_seen": str  # формат: "Oct 27 2025" (НЕ YYYY-MM-DD!)
    },
    "video_2": {...},
    "video_3": {...}
}
```

---

### 4. **Configuration Manager** (config.py)
**Назначение:** Управление настройками и конфигурацией

**Содержимое:**
- URL-адреса (Pipiads, Google Sheets)
- **Email:** aiagentforcosmobeauty@gmail.com
- **Пароль:** )ZRg8,tbOtsR>)U (начинается со скобки, без пробелов)
- **Google Sheet ID:** 1VJMixODvnIPBf7EjFoJ8XMH1lepycVlXREKQI7MVxWQ
- **Название листа:** "шаблон выгрузуи 1.0" (ИМЕННО это название!)
- Google Sheets credentials (путь к JSON ключу)
- Параметры фильтрации (impression threshold: 50k, приоритет 100k, период: 7 дней)
- Настройки браузера (headless/headful, timeout)
- Retry настройки

**Форматы:**
- `.env` файл для секретов (не коммитится)
- `config.yaml` для настроек

---

### 5. **Error Handler & Logger** (logger.py)
**Назначение:** Логирование и обработка ошибок

**Функционал:**
- Логирование всех действий
- Сохранение скриншотов при ошибках
- Retry механизм для сетевых ошибок
- Сохранение прогресса (resume после сбоя)
- Уведомления о критических ошибках

**Технологии:**
- `logging` (стандартная библиотека)
- `pytest-html` или аналог для отчетов (опционально)

---

### 6. **Data Validator** (validator.py)
**Назначение:** Валидация данных перед записью

**Проверки:**
- Формат даты ("Oct 27 2025" - не преобразовывать!)
- Impression >= 50,000 (приоритет >= 100,000)
- **Дата не старше 7 дней** - фильтрация в коде через `parseVideoDate()` и сравнение с `today - 7 days`
- Обязательные поля заполнены (если нет - "N/A")
- Корректность URL-адресов
- Столбец Audience содержит возраст + платформу (формат: "35-45 Android")

---

## 🔄 Workflow (последовательность работы)

```
1. Инициализация
   ├─ Загрузка конфигурации
   ├─ Инициализация браузера
   └─ Подключение к Google Sheets

2. Авторизация на Pipiads
   ├─ Проверка сохраненной сессии
   ├─ Если нет → авторизация
   └─ Сохранение cookies

3. Установка периода (7 дней)
   ├─ Переход на страницу товаров
   ├─ Выбор "7 дней" в фильтре
   └─ Ожидание загрузки

4. Цикл по товарам (первые 3 товара)
   ├─ Для каждого товара (индекс 0, 1, 2):
   │   ├─ Переход на страницу товара через прямую загрузку URL (window.loadUrl)
   │   ├─ Извлечение Product Name, Category
   │   ├─ Переход в TikTok Ads блок
   │   ├─ Установка сортировки "First seen" в dropdown
   │   ├─ Извлечение списка видео со страницы
   │   ├─ **Фильтрация в КОДЕ:**
   │   │   ├─ Парсинг дат через parseVideoDate("Oct 27 2025")
   │   │   ├─ Проверка: дата >= (сегодня - 7 дней)
   │   │   ├─ Приоритет: impression >= 100k, потом >= 50k
   │   │   └─ Выбор топ-3 видео
   │   ├─ Для каждого видео:
   │   │   ├─ Переход на страницу ad-search через window.loadUrl(adSearchUrl)
   │   │   ├─ Извлечение данных (особенно "Показы", не "Нравится"!)
   │   │   ├─ Валидация данных
   │   │   └─ Обработка отсутствующих полей ("N/A")
   │   └─ Запись в Google Sheets:
   │       ├─ Строка = productIndex + 3 (строка 3, 4, 5)
   │       ├─ Лист "шаблон выгрузуи 1.0"
   │       ├─ Столбец A: номер товара (productIndex + 1)
   │       ├─ Столбцы B, D, E, F-Z: данные товара и видео
   │       └─ Столбец C: НЕ ТРОГАТЬ!
   └─ Логирование прогресса

5. Завершение
   ├─ Закрытие браузера
   ├─ Финальный отчет
   └─ Сохранение состояния
```

---

## 🛠 Технологический стек

### Основные библиотеки:
```python
playwright==1.40.0          # Браузерная автоматизация
google-api-python-client    # Google Sheets API
google-auth                 # Аутентификация Google
google-auth-oauthlib        # OAuth2 flow
python-dotenv               # Управление .env
pyyaml                      # Конфигурация YAML
beautifulsoup4              # HTML парсинг (опционально)
python-dateutil             # Работа с датами
```

### Дополнительные:
```python
pytest                      # Тестирование (опционально)
black                       # Форматирование кода
pylint                      # Линтинг
```

---

## 📁 Структура проекта

```
ProductRadar/
├── src/
│   ├── __init__.py
│   ├── main.py                 # Точка входа
│   ├── browser_manager.py      # Управление браузером
│   ├── parser_engine.py        # Парсинг данных
│   ├── sheets_writer.py        # Запись в Google Sheets
│   ├── config.py               # Конфигурация
│   ├── logger.py                # Логирование
│   └── validator.py            # Валидация данных
├── config/
│   ├── config.yaml             # Настройки
│   └── credentials.json        # Google API ключ (не коммитится)
├── logs/                       # Логи
├── screenshots/                # Скриншоты при ошибках
├── .env                        # Секреты (не коммитится)
├── requirements.txt            # Зависимости
├── README.md                   # Документация
└── TECHNICAL_PLAN.md           # Этот файл
```

---

## 🔒 Безопасность

### Что НЕ коммитится:
- `.env` файл (логин/пароль Pipiads)
- `credentials.json` (Google API ключ)
- `cookies.json` (сессия браузера)

### Что добавить в .gitignore:
```
.env
credentials.json
cookies.json
logs/
screenshots/
__pycache__/
*.pyc
```

---

## ⚡ Оптимизации и надежность

### 1. **Retry механизм**
- 3 попытки для сетевых ошибок
- Экспоненциальная задержка между попытками
- Разные стратегии для разных типов ошибок

### 2. **Сохранение прогресса**
- Сохранение ID обработанных товаров
- Возможность продолжения после сбоя
- Пропуск уже обработанных товаров

### 3. **Асинхронность**
- Параллельная обработка нескольких товаров (если не блокирует сайт)
- Асинхронные запросы к Google Sheets API

### 4. **Задержки между запросами**
- Random delay (2-5 секунд) между действиями
- Имитация человеческого поведения
- Избежание блокировок

### 5. **Обработка edge cases**
- Видео меньше 3 (заполнение N/A для отсутствующих видео)
- Отсутствие сценария/hook/age/country (заполнение "N/A")
- Пустые поля (заполнение "N/A")
- Видео старше 7 дней (фильтрация в коде, не использовать)
- Изменение структуры сайта (fallback на альтернативные селекторы)
- **КРИТИЧНО:** Неправильное извлечение impressions (путаница "Показы" vs "Нравится") - использовать специфичные промпты/селекторы

---

## 📊 Мониторинг и отчетность

### Логирование:
- Уровни: DEBUG, INFO, WARNING, ERROR
- Детальное логирование каждого шага
- Статистика: товаров обработано, ошибок, времени выполнения

### Отчеты:
- JSON/CSV отчет о собранных данных
- HTML отчет с визуализацией (опционально)
- Уведомления о критических ошибках

---

## 🚀 Запуск и развертывание

### Локально:
```bash
pip install -r requirements.txt
playwright install chromium
python src/main.py
```

### На сервере (VPS):
- Docker контейнер (опционально)
- Systemd service для автоматического запуска
- Cron для запуска по расписанию

---

## 💰 Стоимость

### Разработка:
- **Время:** 5-7 дней (с тестированием)
- **Сложность:** Средняя

### Эксплуатация:
- **Библиотеки:** Бесплатно (все open-source)
- **Google Sheets API:** Бесплатно (до 300 запросов/100 секунд)
- **VPS:** $10-20/месяц (если нужен постоянный запуск)
- **Итого:** ~$15-25/месяц

---

## ⚠️ Риски и их митигация

### 1. **Изменение структуры сайта Pipiads**
- **Риск:** Селекторы могут перестать работать
- **Митигация:** 
  - Использование более устойчивых селекторов (data-атрибуты, ID)
  - Fallback селекторы
  - Регулярное тестирование

### 2. **Блокировка IP/аккаунта**
- **Риск:** Слишком частые запросы
- **Митигация:**
  - Задержки между запросами
  - Headless режим с user-agent rotation
  - Использование прокси (если необходимо)

### 3. **Ограничения Google Sheets API**
- **Риск:** Превышение лимитов
- **Митигация:**
  - Batch запросы (запись нескольких строк за раз)
  - Rate limiting
  - Мониторинг квот

### 4. **Недостаточно видео для товара**
- **Риск:** Меньше 3 видео с impression >= 50k в пределах 7 дней
- **Митигация:**
  - Заполнение "N/A" для отсутствующих видео
  - Логирование таких случаев
  - Возможность снижения порога impression (опционально)

### 5. **Неправильное извлечение impressions**
- **Риск:** Путаница между "Показы" (impressions) и "Нравится" (likes)
- **Митигация:**
  - Использование очень специфичных селекторов/промптов
  - Указание, что нужно большое число (типа 15.1K), а не маленькое
  - Валидация: impressions должны быть >= 50,000

### 6. **Неправильная работа с Google Sheets**
- **Риск:** Использование GID вместо имени листа, перезапись строки 2
- **Митигация:**
  - Всегда использовать имя листа "шаблон выгрузуи 1.0" (не GID)
  - rowIndex = productIndex + 3 (не +2)
  - Никогда не трогать столбец C

---

## ✅ Критерии успеха

1. **Надежность:** 95%+ успешных парсингов без ошибок
2. **Скорость:** Обработка 1 товара за 2-3 минуты
3. **Точность:** 100% соответствие данных из ТЗ
4. **Автоматизация:** Полностью автоматический процесс без ручного вмешательства
5. **Восстанавливаемость:** Возможность продолжить после сбоя

---

## 📝 Следующие шаги

1. ✅ Утверждение технического плана
2. ✅ Детальное ТЗ сохранено в `TZ_DETAILED.md`
3. Настройка Google Sheets API (получение credentials)
4. Разработка базовой структуры проекта
5. Реализация Browser Manager с авторизацией
6. Реализация Parser Engine с фильтрацией дат в коде
7. Реализация функции parseVideoDate() для работы с датами
8. Интеграция с Google Sheets (лист "шаблон выгрузуи 1.0")
9. Тестирование на реальных данных (3 товара)
10. Проверка фильтрации дат (только последние 7 дней)
11. Проверка извлечения impressions ("Показы", не "Нравится")
12. Оптимизация и доработка
13. Развертывание на сервере (опционально)

## 📋 Дополнительные требования из ТЗ

### Критичные моменты:
1. **Название листа Google Sheets:** "шаблон выгрузуи 1.0" (не GID!)
2. **Строки:** 3, 4, 5 (для товаров 1, 2, 3) - НЕ трогать строку 2
3. **Столбцы:** A (номер), B (Product Name), C (НЕ ТРОГАТЬ), D-Z (данные)
4. **Формат даты:** "Oct 27 2025" (не преобразовывать в YYYY-MM-DD)
5. **Фильтрация дат:** В КОДЕ, не в селекторах/промптах
6. **Impressions:** Извлекать "Показы" (большое число), не "Нравится" (маленькое)
7. **Навигация:** Использовать прямую загрузку URL (window.loadUrl/page.goto)
8. **Приоритет видео:** Сначала >= 100k impressions, потом >= 50k
9. **Отсутствующие данные:** Заполнять "N/A", не оставлять пусто
10. **Audience столбец:** Формат "35-45 Android" (возраст + платформа)

---

## 🎯 Итоговая рекомендация

**Выбранный вариант: Playwright + Google Sheets API + Python**

Это оптимальное решение, которое:
- ✅ Полностью автоматизирует процесс
- ✅ Работает с динамическим JS-контентом
- ✅ Надежно обрабатывает авторизацию и навигацию
- ✅ Прямо записывает в Google Sheets без ручного копирования
- ✅ Легко масштабируется и поддерживается
- ✅ Минимальные финансовые затраты

**Готовность к реализации:** 100%
