# Руководство по использованию ProductRadar Agent

## 📋 Описание

ProductRadar Agent - это AI-агент для автоматического поиска вирусных косметических товаров на различных платформах (Douyin, TikTok, Xiaohongshu и др.).

## 🎯 Возможности

- ✅ Поиск продуктов на 4+ платформах
- ✅ Приоритет Douyin (抖音) - основной источник 20-30 продуктов
- ✅ Автоматический перевод с китайского на русский
- ✅ Расчет метрик (ER, UGC, trendScore)
- ✅ Дедупликация продуктов
- ✅ Экспорт в CSV (UTF-8)
- ✅ Автоматическая загрузка на Google Drive

## 🚀 Быстрый старт

### 1. Настройка окружения

```bash
# Активировать виртуальное окружение
poetry shell

# Или с venv
.\venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac
```

### 2. Настройка .env файла

Создайте файл `.env` на основе `.env.example`:

```env
# API Keys для AI (нужен хотя бы один)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Настройки агента
AGENT_MODEL=gpt-3.5-turbo
AGENT_TEMPERATURE=0.3
```

### 3. Запуск агента

```bash
# Базовый запуск (30-50 продуктов, 25 с Douyin)
python scripts/run_agent.py

# С кастомными параметрами
python scripts/run_agent.py --min-products 40 --target-products 60 --douyin-target 30

# С Google Drive интеграцией
python scripts/run_agent.py --google-creds path/to/credentials.json
```

## ⚙️ Параметры запуска

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--min-products` | Минимальное количество продуктов | 30 |
| `--target-products` | Целевое количество продуктов | 50 |
| `--douyin-target` | Целевое количество с Douyin | 25 |
| `--no-douyin-priority` | Отключить приоритет Douyin | False |
| `--google-creds` | Путь к Google credentials | None |

## 📊 Выходные данные

### CSV файл

Файл сохраняется как `radar_YYYY-MM-DD_HHmm.csv` и содержит:

- Дата обнаружения
- Платформа
- Категория (лицо/тело/волосы/декор)
- Название товара (оригинал и перевод)
- Ссылки на товар и продавца
- Метрики (просмотры, ER, UGC%)
- Топ-хуки и офферы (оригинал и перевод)
- trendScore и приоритет

### Google Drive

Если настроены credentials, файл автоматически загружается в папку:
`https://drive.google.com/drive/folders/1fGCUHtUmNZlJyTjmbLmPjcfeOrDu9icq`

## 🔧 Настройка Google Drive

### 1. Создание Service Account

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Drive API
4. Создайте Service Account
5. Скачайте JSON credentials

### 2. Предоставление доступа

1. Откройте папку на Google Drive
2. Нажмите "Поделиться"
3. Добавьте email Service Account (из JSON файла)
4. Дайте права "Редактор"

### 3. Использование

```bash
python scripts/run_agent.py --google-creds service-account.json
```

## 📚 Структура проекта

```
src/
├── agents/
│   ├── base_agent.py           # Базовый класс агента
│   └── product_radar_agent.py  # Главный агент
├── parsers/
│   ├── base_parser.py          # Базовый парсер
│   ├── douyin_parser.py        # Парсер Douyin
│   ├── tiktok_parser.py        # Парсер TikTok
│   └── xiaohongshu_parser.py   # Парсер Xiaohongshu
├── models/
│   └── product.py              # Модели данных
├── utils/
│   ├── translator.py           # Перевод текстов
│   ├── metrics.py              # Расчет метрик
│   ├── exporter.py             # Экспорт и загрузка
│   └── logger.py               # Логирование
└── config/
    └── settings.py             # Настройки
```

## 🎯 Алгоритм работы агента

### Шаг 1: Сбор продуктов
- Параллельный поиск на всех платформах
- Douyin - приоритет, целевой объём 20-30 продуктов
- TikTok (US, EU) - по 10-15 продуктов
- Xiaohongshu - 10-15 продуктов

### Шаг 2: Обработка данных
- Перевод названий, хуков, офферов с китайского на русский
- Валидация данных
- Извлечение метрик

### Шаг 3: Дедупликация
- Удаление дубликатов по SKU/URL
- Сохранение лучшей версии при совпадениях

### Шаг 4: Расчет метрик
- ER (Engagement Rate)
- Impulse 72h (импульс за 72 часа)
- UGC share (доля пользовательского контента)
- Recency score (свежесть)
- trendScore (итоговая оценка 0-100)

### Шаг 5: Финализация
- Сортировка по trendScore
- Определение приоритетов (A/B/C)
- Ограничение до целевого количества

### Шаг 6: Экспорт
- Создание CSV файла
- Загрузка на Google Drive (если настроено)
- Генерация отчета

## 📈 Метрики и скоринг

### trendScore (0-100)

**Для Douyin:**
```
trendScore = 0.45×Impulse72h + 0.30×UGCshare + 0.10×ERz + 0.10×RecencyScore + 0.05×EaseComposite
```

**Для остальных платформ:**
```
trendScore = 0.35×Impulse72h + 0.25×UGCshare + 0.15×ERz + 0.15×RecencyScore + 0.10×EaseComposite
```

### Приоритеты

- **A** - trendScore ≥ 75 (высокий приоритет)
- **B** - 45 ≤ trendScore < 75 (средний приоритет)
- **C** - trendScore < 45 (низкий приоритет)

## 🔍 Примеры использования

### Пример 1: Стандартный запуск

```bash
python scripts/run_agent.py
```

Результат: 30-50 продуктов, 25 с Douyin

### Пример 2: Больше продуктов с Douyin

```bash
python scripts/run_agent.py --douyin-target 40 --target-products 70
```

Результат: до 70 продуктов, 40 с Douyin

### Пример 3: Без приоритета Douyin

```bash
python scripts/run_agent.py --no-douyin-priority --target-products 60
```

Результат: 60 продуктов, равномерно со всех платформ

### Пример 4: С Google Drive

```bash
python scripts/run_agent.py --google-creds google-credentials.json
```

Результат: CSV файл + автоматическая загрузка на Drive

## 🐛 Отладка

### Логи

Логи сохраняются в `logs/agent.log`. Для просмотра:

```bash
# Windows
type logs\agent.log

# Linux/Mac
tail -f logs/agent.log
```

### Распространенные проблемы

**Проблема:** API ключи не найдены
```
Решение: Добавьте OPENAI_API_KEY или ANTHROPIC_API_KEY в .env файл
```

**Проблема:** Не удается получить доступ к платформе
```
Решение: Некоторые платформы требуют авторизации. Агент автоматически пропускает их.
```

**Проблема:** Ошибка при загрузке на Google Drive
```
Решение: Проверьте путь к credentials и права доступа к папке
```

## 📝 Развитие

### TODO для улучшения:

- [ ] Реальная имплементация парсеров (сейчас заглушки)
- [ ] Поддержка YouTube Shorts и Instagram Reels
- [ ] Кэширование результатов
- [ ] Rate limiting для API
- [ ] Proxy поддержка
- [ ] Web UI для просмотра результатов
- [ ] Telegram уведомления
- [ ] Scheduled запуски (cron/scheduler)

## 🤝 Вклад

См. [CONTRIBUTING.md](CONTRIBUTING.md) для деталей.

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE)

---

**Контакты:** [GitHub Issues](https://github.com/YOUR_USERNAME/ProductRadar/issues)

