# Алгоритм извлечения метрик - подробное описание

## 📋 Содержание
1. [Метрики на странице товара](#метрики-на-странице-товара)
2. [Метрики на странице ad-search](#метрики-на-странице-ad-search)
3. [Проблемы и их решения](#проблемы-и-их-решения)

---

## 📄 МЕТРИКИ НА СТРАНИЦЕ ТОВАРА

### 1. Product Name (Название товара)

**Метод 1: Поиск через селекторы Playwright**
```python
Селекторы (в порядке приоритета):
- 'h1:first-of-type'
- 'h1[class*="product"]'
- 'h1[class*="title"]'
- '[class*="product-title"]'
- '[class*="product-name"]'
- 'h1'
- 'h2:first-of-type'
```

**Метод 2: JavaScript поиск по DOM**
```javascript
1. Ищем h1 элемент
2. Если не найден или слишком короткий, ищем в элементах с классом "product"
3. Ищем самый длинный заголовок (h1, h2, h3), пропуская короткие тексты (< 20 символов)
4. Ищем в текстовых блоках (p, div, span) длинные тексты (> 30 символов)
5. Ищем в meta-тегах: meta[property="og:title"]
```

**Фильтрация:**
- Пропускаем тексты, содержащие: 'tiktok shop product', 'ad analysis', 'category', 'commission'
- Убираем префикс "TikTok Shop Product Detail:"
- Убираем "TikTok Shop Product" из начала и конца
- Минимальная длина: 5 символов

**Результат:** Строка с названием товара или "N/A"

---

### 2. Category (Категория)

**Метод 1: Поиск через селекторы**
```python
Селекторы:
- '[class*="category"]'
- '[class*="tag"]'
- 'text=/Category/i'
- 'text=/Категория/i'
```

**Метод 2: JavaScript поиск**
```javascript
1. Ищем все элементы с текстом "Category" или "Категория"
2. Извлекаем текст после "Category:" или "Категория:"
3. Убираем "Commission Rate" и все после него
4. Убираем проценты (например "15.00%")
```

**Очистка:**
- Убираем "Category:" и "Категория:"
- Убираем "Commission Rate: ..." и "Комиссия: ..."
- Убираем проценты: `\d+\.?\d*\s*%`
- Нормализуем разделители: `\s*>\s*` → ` > `
- Максимальная длина: 100 символов

**Результат:** Строка с категорией (например "Beauty & Personal Care > Skincare > Skin Care Kits") или "N/A"

---

### 3. Impression из карточки видео (на странице товара)

**Метод 1: Поиск с явным упоминанием "Impression" или "Показы"**
```python
Regex паттерны:
- r'Impression[:\s]+([\d.,]+[KM]?)'  # "Impression: 6.5K"
- r'([\d.,]+[KM]?)\s*Impression'     # "6.5K Impression"
- r'Показы[:\s]+([\d.,]+[KM]?)'      # "Показы: 6.5K"
- r'([\d.,]+[KM]?)\s*Показы'         # "6.5K Показы"
```

**Метод 2: Поиск больших чисел (fallback)**
```python
1. Ищем все числа в формате "X.XK" или "X.XM" в тексте карточки
2. Фильтруем только числа >= 1000 (1K)
3. Берем самое большое число (скорее всего это impression)
```

**Парсинг:**
- Используется `validator.parse_impressions()` для преобразования "6.5K" → 6500
- Минимальное значение: 1000 (1K)

**Результат:** Число (int) или 0

---

### 4. First seen из карточки видео (на странице товара)

**Regex паттерны:**
```python
- r'First\s+seen[:\s]+([A-Z][a-z]{2}\s+\d{1,2}\s+\d{4})'  # "First seen: Nov 02 2025"
- r'Впервые\s+замечено[:\s]+([A-Z][a-z]{2}\s+\d{1,2}\s+\d{4})'  # "Впервые замечено: Nov 02 2025"
- r'([A-Z][a-z]{2}\s+\d{1,2}\s+\d{4})\s*-\s*[A-Z][a-z]{2}\s+\d{1,2}\s+\d{4}'  # "Nov 02 2025 - Nov 05 2025"
- r'([A-Z][a-z]{2}\s+\d{1,2}\s+\d{4})'  # "Nov 02 2025" (любая дата)
```

**Обработка:**
- Берется ПЕРВАЯ дата из диапазона (если есть диапазон)
- Валидация через `validator.parse_video_date()`

**Результат:** Строка в формате "Nov 02 2025" или None

---

### 5. ad_search_url из карточки видео

**Метод:**
```python
1. Ищем ссылку: a[href*="/ad-search/"]
2. Получаем атрибут href
3. Формируем полный URL:
   - Если начинается с "/" → "https://www.pipiads.com" + href
   - Если начинается с "http" → оставляем как есть
   - Иначе → "https://www.pipiads.com/" + href
```

**Результат:** URL строки или None

---

## 🎬 МЕТРИКИ НА СТРАНИЦЕ AD-SEARCH

### 1. TikTok Link (Ссылка) - ✅ ИЗМЕНЕНО: теперь пишем ad-search URL

**НОВЫЙ АЛГОРИТМ:**
```python
1. Получаем текущий URL страницы: self.page.url
2. Проверяем, содержит ли URL "/ad-search/"
3. Если да:
   - Форматируем URL: добавляем "/" в конец, если его нет
   - Сохраняем в video_data["tiktok_link"]
   - Формат: "https://www.pipiads.com/ad-search/830e981c72e63bf0a587/"
4. Если нет: устанавливаем "N/A"
```

**Результат:** URL ad-search страницы (например "https://www.pipiads.com/ad-search/830e981c72e63bf0a587/") или "N/A"

---

### 2. Impressions (Показы) - ✅ ИСПРАВЛЕНО: используется BeautifulSoup

**НОВЫЙ АЛГОРИТМ (ТОПОРНЫЙ МЕТОД):**

**Метод 1: Парсинг HTML с BeautifulSoup**
```python
1. Получаем HTML страницы: await self.page.content()
2. Создаем BeautifulSoup объект: BeautifulSoup(html_content, 'html.parser')
3. Ищем все элементы с текстом, содержащим "Impression", "Показ", "Показы", "Impressions"
4. Для каждого найденного элемента:
   - Получаем родительский элемент
   - Извлекаем весь текст родительского элемента
   - Ищем число через regex: r'Impression[:\s]*([\d.,]+[KM]?)' или r'([\d.,]+[KM]?)\s*Impression'
   - Парсим число через validator.parse_impressions()
   - Фильтруем: 1000 <= numValue <= 1000000000 (от 1K до 1B)
```

**Метод 2: Поиск в разделе "Data" или "Данные"**
```python
1. Ищем элементы с текстом "Data" или "Данные"
2. В родительском контейнере ищем "Impression" или "Показ"
3. Извлекаем число через regex
4. Фильтруем: 1000 <= numValue <= 1000000000
```

**Fallback:**
- Если не найдено на странице ad-search, используется значение из карточки (`_original_impression`)

**Результат:** Строка в формате "170.6K" или "N/A"

---

### 3. Script (Сценарий) - ✅ ИСПРАВЛЕНО: используется BeautifulSoup

**НОВЫЙ АЛГОРИТМ (ТОПОРНЫЙ МЕТОД):**

**Метод 1: Парсинг HTML с BeautifulSoup через H1/H2**
```python
1. Получаем HTML страницы: await self.page.content()
2. Создаем BeautifulSoup объект
3. Ищем все H1 элементы: soup.find_all('h1')
4. Для каждого H1 проверяем, содержит ли он "Script Analysis"
5. Если найден:
   - Ищем родительский контейнер: h1.find_parent(['section', 'div', 'article'])
   - В контейнере ищем H2: container.find_all('h2')
   - Для каждого H2 проверяем, содержит ли он "Scripts" или "Script"
   - Извлекаем текст после H2:
     * Способ 1: h2.find_next_sibling().get_text()
     * Способ 2: parent.get_text() после позиции H2
6. Очищаем текст через _clean_script_text()
```

**Метод 2: Поиск элементов с текстом "Script"**
```python
1. Ищем все элементы с текстом, содержащим "Scripts", "Script", "Сценарий", "Transcript"
2. Для каждого элемента:
   - Получаем родительский элемент
   - Разделяем текст по ключевому слову
   - Извлекаем текст после ключевого слова
   - Очищаем через _clean_script_text()
```
```python
Ключевые слова: "Scripts", "Script", "Сценарий", "Transcript", "Анализ транскрипта"
1. Ищем локатор: text=/Script/i
2. Извлекаем текст из:
   - Родительского элемента
   - Следующего sibling элемента
   - Дочерних элементов (p, div, span, pre)
```

**Метод 3: JavaScript TreeWalker (fallback)**
```javascript
1. Проходим по всем текстовым узлам DOM
2. Ищем родительские элементы, содержащие "Script"
3. Извлекаем текст после ключевого слова
4. Фильтруем стоп-слова: 'Hook', 'Target Audience', 'First seen', 'Impressions', 'Country'
```

**Метод 4: HTML парсинг (fallback)**
```python
Regex паттерны:
- r'(?:Script|Сценарий|Transcript)[:\s]*([^<]+?)(?:Hook|Target Audience|...)'
- r'(?:Script|Сценарий|Transcript)[:\s]*([^<]{50,500})'
```

**Очистка (`_clean_script_text`):**
- Убираем текст кнопок: "More Detail", "See product", "See TT post"
- Убираем стоп-слова: "Hook", "Target Audience", "First seen", "Impressions", "Country"
- Убираем теги (строки, начинающиеся с #)
- Убираем строки, содержащие "Tags", "Script", "Hooks"
- Фильтруем футер/меню, метаданные, промо-тексты
- Минимальная длина: 20 символов

**Результат:** Строка со скриптом или "N/A"

---

### 4. Hook (Хук) - ✅ ИСПРАВЛЕНО: используется BeautifulSoup

**НОВЫЙ АЛГОРИТМ (ТОПОРНЫЙ МЕТОД):**

**Метод 1: Парсинг HTML с BeautifulSoup через H1/H2**
```python
1. Получаем HTML страницы: await self.page.content()
2. Создаем BeautifulSoup объект
3. Ищем все H1 элементы: soup.find_all('h1')
4. Для каждого H1 проверяем, содержит ли он "Script Analysis"
5. Если найден:
   - Ищем родительский контейнер
   - В контейнере ищем H2 с текстом "Hooks" или "Hook"
   - Извлекаем текст после H2:
     * Способ 1: h2.find_next_sibling().get_text()
     * Способ 2: parent.get_text() после позиции H2
6. Очищаем от стоп-слов и метаданных
```

**Метод 2: Поиск элементов с текстом "Hook"**
```python
1. Ищем все элементы с текстом, содержащим "Hooks", "Hook", "Хуки", "Хук"
2. Для каждого элемента:
   - Получаем родительский элемент
   - Разделяем текст по ключевому слову
   - Извлекаем текст после ключевого слова
   - Очищаем от стоп-слов и метаданных
```
```python
1. Находим элемент с текстом "Script"
2. Ищем следующий элемент, содержащий "Hook" или "Hooks"
3. Извлекаем текст после "Hook" или "Hooks"
```

**Очистка:**
- Убираем метаданные: "Quality", "Size", "Resolution", "--"
- Убираем стоп-слова: "Target Audience", "First seen", "Impressions", "Country"
- Убираем множественные переносы строк
- Длина: 5-500 символов

**Результат:** Строка с хуком или "N/A"

---

### 5. Audience Age (Возраст аудитории) - ✅ ИСПРАВЛЕНО: используется BeautifulSoup

**НОВЫЙ АЛГОРИТМ (ТОПОРНЫЙ МЕТОД):**
```python
1. Получаем HTML страницы: await self.page.content()
2. Создаем BeautifulSoup объект
3. Ищем элементы с текстом, содержащим "Audience", "Аудитория", "Target Audience", "Целевая аудитория"
4. Для каждого найденного элемента:
   - Получаем родительский элемент
   - Извлекаем весь текст родительского элемента
   - Ищем возраст через regex:
     * r'Возраст[:\s]+(\d{1,2}-\d{1,2})'
     * r'Age[:\s]+(\d{1,2}-\d{1,2})'
     * r'(\d{1,2}-\d{1,2})'  # Просто возраст
5. Возвращаем первое найденное значение
```

**Результат:** Строка в формате "25-35" или "N/A"

---

### 6. Country (Страна) - ✅ ИСПРАВЛЕНО: используется BeautifulSoup

**НОВЫЙ АЛГОРИТМ (ТОПОРНЫЙ МЕТОД):**
```python
1. Получаем HTML страницы: await self.page.content()
2. Создаем BeautifulSoup объект
3. Ищем элементы с текстом, содержащим "Country/Region", "Страна/регион", "Country", "Страна", "Region", "Регион"
4. Для каждого найденного элемента:
   - Получаем родительский элемент
   - Извлекаем весь текст родительского элемента
   - Ищем страну через regex паттерны:
     * r'United States(?:\([0-9]+\))?'
     * r'Philippines(?:\([0-9]+\))?'
     * r'Russia(?:\([0-9]+\))?'
     * и т.д.
5. Убираем (1) и т.д. из результата
```

**Результат:** Строка с названием страны (например "Philippines") или "N/A"

---

### 7. First seen (Дата первого показа) - ✅ ИСПРАВЛЕНО: используется BeautifulSoup

**НОВЫЙ АЛГОРИТМ (ТОПОРНЫЙ МЕТОД):**
```python
1. Получаем HTML страницы: await self.page.content()
2. Создаем BeautifulSoup объект
3. Ищем элементы с текстом, содержащим "First seen - Last seen", "First seen", "Впервые замечено", "First Seen"
4. Для каждого найденного элемента:
   - Получаем родительский элемент
   - Извлекаем весь текст родительского элемента
   - Ищем дату после ключевого слова:
     * Разделяем текст по ключевому слову
     * Берем только до "~" (если есть диапазон)
     * Ищем дату через regex:
       - r'([A-Z][a-z]{2}\s+\d{1,2}\s+\d{4})'  # "Oct 27 2025"
       - r'([A-Z][a-z]{2}\s+\d{1,2},\s+\d{4})'  # "Oct 27, 2025"
       - r'(\d{1,2}\s+[A-Z][a-z]{2}\s+\d{4})'  # "27 Oct 2025"
5. Убираем запятую из даты, если есть
```

**Результат:** Строка в формате "Oct 27 2025" или "N/A"

---

## ⚠️ ПРОБЛЕМЫ И ИХ РЕШЕНИЯ

### Проблема 1: Impressions пишется 9.4M, а такого нет - ✅ РЕШЕНО

**Решение:**
1. ✅ Перешли на парсинг HTML через BeautifulSoup (топорный метод)
2. ✅ Ищем все элементы с текстом "Impression" или "Показ" в HTML
3. ✅ Извлекаем число из родительского элемента
4. ✅ Фильтруем значения: 1000 <= numValue <= 1000000000 (от 1K до 1B)
5. ✅ Если не найдено, используем значение из карточки (fallback)

**Преимущества нового подхода:**
- Работает напрямую с HTML, не зависит от динамической загрузки
- Проще отлаживать - можно посмотреть HTML файл
- Более надежный поиск по тексту

---

### Проблема 2: Script и Hook не находятся правильно - ✅ РЕШЕНО

**Решение:**
- ✅ Перешли на парсинг HTML через BeautifulSoup
- ✅ Ищем H1 "Script Analysis" → H2 "Scripts"/"Hooks" через BeautifulSoup
- ✅ Извлекаем текст после H2 из следующего sibling или родительского элемента
- ✅ Добавлено сохранение HTML для анализа
- ✅ Улучшена очистка текста от кнопок и стоп-слов

**Преимущества нового подхода:**
- Работает напрямую с HTML структурой
- Проще найти нужные элементы через find_all()
- Не зависит от динамической загрузки контента

---

## 📝 ЗАМЕТКИ

1. **Все метрики извлекаются через BeautifulSoup** - топорный метод парсинга HTML
2. **HTML файлы сохраняются** в папку `html_dumps/` для анализа
3. **Детальное логирование** - все шаги логируются для отладки
4. **Валидация данных** - используется `validator.py` для проверки и форматирования данных
5. **TikTok ссылка изменена** - теперь пишется ad-search URL вместо ссылки на видео

---

## ✅ ВЫПОЛНЕННЫЕ ИЗМЕНЕНИЯ

1. ✅ Перешли на парсинг HTML через BeautifulSoup для всех метрик
2. ✅ Изменили запись TikTok ссылки - теперь пишется ad-search URL
3. ✅ Упростили алгоритм извлечения данных - используем простой поиск по тексту в HTML
4. ✅ Улучшили фильтрацию impressions - диапазон 1K-1B
5. ✅ Сохраняем HTML файлы для анализа

---

## 🔍 ПРЕИМУЩЕСТВА НОВОГО ПОДХОДА

1. **Простота** - работаем напрямую с HTML, не зависим от динамической загрузки
2. **Надежность** - BeautifulSoup надежно парсит HTML структуру
3. **Отладка** - можно посмотреть HTML файл и найти нужные элементы
4. **Скорость** - парсинг HTML быстрее, чем сложные селекторы Playwright

